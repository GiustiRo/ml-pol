import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'ml.pol',
  appName: 'ml-pol',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
